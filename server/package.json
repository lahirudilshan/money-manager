{
  "name": "money-manager-catalog-api",
  "version": "1.0.0",
  "private": true,
  "description": "Shared merchant→category catalog. Deploys separately from the Expo app and owns the DATABASE_URL the app must never hold.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "schema": "node --env-file-if-exists=.env apply-schema.mjs",
    "seed": "node --env-file-if-exists=.env seed.mjs",
    "test:e2e": "node --env-file-if-exists=.env e2e.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "next": "16.2.12",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.12",
    "typescript": "^5"
  }
}
